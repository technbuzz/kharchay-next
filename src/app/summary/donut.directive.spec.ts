import { DonutDirective } from './donut.directive';

describe('DonutDirective', () => {
  it('should create an instance', () => {
    const directive = new DonutDirective();
    expect(directive).toBeTruthy();
  });
});
