import { commonDataAdapters } from './common-data-adapters';

describe('commonDataAdapters', () => {
  it('should work', () => {
    expect(commonDataAdapters()).toEqual('common-data-adapters');
  });
});
