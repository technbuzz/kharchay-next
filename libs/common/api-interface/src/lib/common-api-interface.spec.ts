import { commonApiInterface } from './common-api-interface';

describe('commonApiInterface', () => {
  it('should work', () => {
    expect(commonApiInterface()).toEqual('common-api-interface');
  });
});
