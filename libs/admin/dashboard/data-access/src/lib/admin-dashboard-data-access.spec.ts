import { adminDashboardDataAccess } from './admin-dashboard-data-access';

describe('adminDashboardDataAccess', () => {
  it('should work', () => {
    expect(adminDashboardDataAccess()).toEqual('admin-dashboard-data-access');
  });
});
